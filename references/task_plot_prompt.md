Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Hick–Hyman Choice Reaction Task
Construct: response selection / information processing
Rows/conditions:
- 2 choices · 1 bit: two centered outline circles, one black filled target; F/J mapping is learned but key labels are not shown on the response screen.
- 4 choices · 2 bits: four centered outline circles, one black filled target; D/F/J/K mapping is learned but key labels are not shown on the response screen.
- 8 choices · 3 bits: eight outline circles across the screen, one black filled target; A/S/D/F/J/K/L/; mapping is learned but key labels are not shown on the response screen.

Timeline phases:
- 2 choices · 1 bit: Fixation (500 ms; no response; central black +) -> Choice response (2000 ms max; keypress; two centered outline circles with one centered black target dot) -> ITI (700 ms; no response; central black +)
- 4 choices · 2 bits: Fixation (500 ms; no response; central black +) -> Choice response (2000 ms max; keypress; four centered outline circles with one centered black target dot) -> ITI (700 ms; no response; central black +)
- 8 choices · 3 bits: Fixation (500 ms; no response; central black +) -> Choice response (2000 ms max; keypress; eight evenly spaced outline circles with one centered black target dot) -> ITI (700 ms; no response; central black +)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per condition or representative trial type.
- Each row contains exactly 3 participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows the visible stimulus or feedback, not internal variable names.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place condition labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Leave a clean blank header band across the top 15-18% of the image. This band is reserved for a fixed title, `Construct: ...` subtitle, and TaskBeacon logo lockup that will be added after generation.
- Keep all participant screens at the same aspect ratio and size.
- Keep circle diameter identical in all rows. The 2-choice circles must not be larger than the 8-choice circles.
- Center every black target dot precisely inside its outline circle.
- Add one small note beneath the timeline collection: `Error or timeout: 500 ms corrective feedback`.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, or timings.
- Do not add people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- If a detail is unknown, omit it rather than guessing.
- Preserve these exact terms where used: `2 choices · 1 bit`, `4 choices · 2 bits`, `8 choices · 3 bits`, `Fixation`, `Choice response`, `ITI`, `500 ms`, `2000 ms max`, `700 ms`, `Error or timeout: 500 ms corrective feedback`.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
